server:
  port: 8026

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0